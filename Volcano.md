# first create file configuration about ts

```cmd
tsc --init
```

- and make this commend in tsconfig
- cerate file package.json `npm init -y`
- to run both ts and js download third-party-module `concurrently`
